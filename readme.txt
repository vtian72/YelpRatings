To run each model, you just need to import the libraries, read in the data and run the custom function k_fold_cross_val.
Then just scroll to the very bottom and run the last code cell which will generate all the model results.

The CSV files are the predictions for each model used.